This calculator is developed using HTML5 and CSS which can evaluate simple arithmetic on integer numbers.
The snapshots which I have uploaded can be open in another tab in order to get the zoom view of the image. 
